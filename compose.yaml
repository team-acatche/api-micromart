services:
  authenticationapi:
    image: authenticationapi
    ports:
      - "60996:8080"
    environment:
      - TOKEN_EXPIRATION_MINUTES=5
    build:
      context: .
      dockerfile: AuthenticationApi/Dockerfile
    networks:
      - network-micromart

  taxcalculatorapi:
    image: taxcalculatorapi
    ports:
      - "60997:8080"
    build:
      context: .
      dockerfile: TaxCalculatorApi/Dockerfile
    networks:
      - network-micromart

  invoicenumbergeneratorapi:
    image: invoicenumbergeneratorapi
    ports:
      - "60998:8080"
    build:
      context: .
      dockerfile: InvoiceNumberGeneratorApi/Dockerfile
    networks:
      - network-micromart

  discountengineapi:
    image: discountengineapi
    ports:
      - "60999:8080"
    build:
      context: .
      dockerfile: DiscountEngineApi/Dockerfile
    networks:
      - network-micromart

  registryservice:
    image: registryservice
    ports:
      - "61000:8080"
    environment:
<<<<<<< HEAD
    - AUTHENTICATION_URI=http://authenticationapi:8080
    - TAX_CALCULATOR_URI=http://taxcalculatorapi:8080
    - INVOICE_NUMBER_GENERATOR_URI=http://invoicenumbergeneratorapi:8080
    - DISCOUNT_ENGINE_URI=http://discountengineapi:8080
=======
      # public URLs — returned to developers via the registry
      - TAX_CALCULATOR_URI=https://tax-apimicromart.acatche.com
      - INVOICE_NUMBER_GENERATOR_URI=https://invoicenumber-apimicromart.acatche.com
      - DISCOUNT_ENGINE_URI=https://discount-apimicromart.acatche.com
      # internal URLs — used by the Blazor server's Execute()
      - TAX_CALCULATOR_INTERNAL_URI=http://taxcalculatorapi:8080
      - INVOICE_NUMBER_GENERATOR_INTERNAL_URI=http://invoicenumbergeneratorapi:8080
      - DISCOUNT_ENGINE_INTERNAL_URI=http://discountengineapi:8080
>>>>>>> 5dbcf43 (feat: add public subdomain routing with internal/external URL separation)
    build:
      context: .
      dockerfile: RegistryService/Dockerfile
    depends_on:
      - authenticationapi
      - discountengineapi
      - invoicenumbergeneratorapi
      - taxcalculatorapi
    networks:
      - network-micromart

  micromart:
    image: micromart
    ports:
      - "61001:8080"
    environment:
      - REGISTRY_URI=http://registryservice:8080
      - REGISTRY_PUBLIC_URI=https://registry-apimicromart.acatche.com
    build:
      context: .
      dockerfile: AcatcheApiMicromart/Dockerfile
    depends_on:
      - registryservice
    networks:
      - network-micromart

networks:
  network-micromart:
<<<<<<< HEAD
    driver: bridge


=======
    driver: bridge
>>>>>>> 5dbcf43 (feat: add public subdomain routing with internal/external URL separation)
